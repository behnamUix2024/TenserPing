<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TenserPingX" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

    </style>
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <style name="CustomDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
    <style name="cardAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="backgroundColor">@android:color/transparent</item>
        <item name="cornerRadius">10dp</item>
        <item name="elevation">4dp</item>
        <item name="textFillColor">@color/black_mat</item>
        <item name="fontFamily">@font/vazir_fd_wol</item>
    </style>
</resources>